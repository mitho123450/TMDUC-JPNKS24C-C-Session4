#include<stdio.h>
 int main(){
 	int number;
 	printf("Nhap so nguyen bat ki :");
 	scanf("%d",&number);
 	if(number%2==0){
 		printf("La so chan");	
	 }
	 else {
	 	printf("La so le");
	 }
	 return 0;
 }

